apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb-pvc
  namespace: chat-app
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  volumeName: mongodb-pv
  resources:
    requests:
      storage: 5Gi
